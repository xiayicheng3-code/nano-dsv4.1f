# Archived notebook builders

These builders reproduce historical notebooks and preserve their original branch/protocol assumptions. They are not current entry points and may require path adjustment if deliberately rerun from this archive directory.

Current builders include `build_final_attention_notebook.py` and `build_moe_buffer_notebook.py`.
